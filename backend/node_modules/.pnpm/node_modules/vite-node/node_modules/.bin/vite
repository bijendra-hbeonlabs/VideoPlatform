#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../vite/bin/vite.js" "$@"
else
  exec node  "$basedir/../../../vite/bin/vite.js" "$@"
fi
# cmd-shim-target=C:/Users/Bijendra Kumar Patel/Downloads/Hbeonlabs Projects/video-display-platform/video-display-platform-local-backup/backend/node_modules/.pnpm/vite-node@3.2.4_@types+node@24.13.3_tsx@4.23.12/node_modules/vite/bin/vite.js
